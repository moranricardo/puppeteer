jobs:
  build:
    runs-on: ubuntu-latest
      env:
        DEBUG: "puppeteer:*"
    steps:
      - uses: actions/checkout@v3
      - name: Instalar dependencias
        run: npm install
      # ... el resto de tus pasos