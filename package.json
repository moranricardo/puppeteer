{
  "name": "puppeteer-in-browser",
  "version": "1.0.0",
  "description": "Automatización con Puppeteer ejecutándose en el navegador",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf out",
    "build": "npm run clean && rollup -c && gzip -k --best -r -f out"
  },
  "keywords": [
    "puppeteer",
    "extension",
    "automation",
    "rollup"
  ],
  "author": "Ricardo Moran Maldonado",
  "license": "MIT",
  "devDependencies": {
    "@rollup/plugin-commonjs": "^29.0.3",
    "@rollup/plugin-node-resolve": "^15.3.1",
    "@rollup/plugin-terser": "^0.4.4",
    "rimraf": "^6.1.3",
    "rollup": "^4.62.4",
    "terser": "^5.30.4"
  }
}
